# Ce dépôt versionne `.env.production` avec cette valeur pour `vite build`.
# Pour un fork / autre API : copier vers `.env.production` ou définir `VITE_API_BASE` sur la plateforme (ex. Railway).
# Sans slash final — aligné sur `getApiBase()` / `connect-src` CSP.
VITE_API_BASE=https://africaqhse-production.up.railway.app
